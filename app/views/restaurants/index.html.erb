<h1>Restaurants</h1>

<ul>
  <% @restaurants.each do |restaurant| %>
    <li>
      <p>👉 <%= link_to restaurant.name, restaurant_path(restaurant) %> |
        <% if restaurant.category == 'italian' %>
          🍕 <%= restaurant.category.capitalize %> Restaurant </p>
        <% elsif restaurant.category == 'chinese' %>
          🍜 <%= restaurant.category.capitalize %> Restaurant </p>
        <% elsif restaurant.category == 'japanese' %>
          🍙 <%= restaurant.category.capitalize %> Restaurant </p>
         <% elsif restaurant.category == 'french' %>
          🥐 <%= restaurant.category.capitalize %> Restaurant </p>
        <% else %>
          🧇 <%= restaurant.category.capitalize %> Restaurant </p>
        <% end %>
      </p>
    </li>
  <% end %>
</ul>

<%= link_to '➕ Add a restaurant', new_restaurant_path%>
